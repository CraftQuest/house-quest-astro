---
export async function getEntry(query: string, url: string) {
    const response = await fetch (url, {
    method: "POST",
    headers: {
        "Content-Type": "application/json",
    },
    body: JSON.stringify({
        query: 
        query
        ,
    }),
});

let json = await response.json();
return json.data.entry;
}

---